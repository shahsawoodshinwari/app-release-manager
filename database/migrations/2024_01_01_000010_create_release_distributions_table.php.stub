<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('release_distributions', function (Blueprint $table) {
            $table->id();

            $table->foreignId('release_id')->constrained('releases');
            $table->foreignId('application_distribution_channel_id')->constrained('application_distribution_channels');
            $table->foreignId('status_id')->constrained('release_distribution_statuses');
            
            $table->string('store_version', 50)->nullable();
            $table->unsignedBigInteger('store_build_number')->nullable();
            $table->text('store_url')->nullable();
            $table->timestamp('published_at')->nullable();
            
            $table->timestamps();

            $table->unique(['release_id', 'application_distribution_channel_id']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('release_distributions');
    }
};
